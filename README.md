# isaacsim-launch

Everything is in [`isaacsim-launch-nebius/`](isaacsim-launch-nebius/README.md): scripts + a
Claude Code skill to launch an Isaac Sim 6.0.1 (WebRTC streaming) + Isaac Lab 3.0 GPU box on Nebius.

```bash
cd isaacsim-launch-nebius && cp .env.example .env && deploy/nebius-up.sh
```
